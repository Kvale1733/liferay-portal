@default = "assertTextNotPresent"
definition {

	function assertTextNotPresent(value1) {
		WaitForSPARefresh();

		selenium.waitForTextNotPresent(${value1});

		selenium.assertTextNotPresent(${value1});

		

		
	}

}