@default = "assertLocation"
definition {

	function assertLocation(value1) {
		WaitForSPARefresh();

		selenium.assertLocation(${value1});

		

		
	}

	function assertPartialLocation(value1) {
		WaitForSPARefresh();

		selenium.assertPartialLocation(${value1});

		

		
	}

}