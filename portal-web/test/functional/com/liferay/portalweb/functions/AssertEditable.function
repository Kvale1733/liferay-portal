@default = "assertEditable"
definition {

	function assertEditable(locator1) {
		WaitForSPARefresh();

		selenium.waitForVisible(${locator1}, "false");

		selenium.waitForEditable(${locator1});

		selenium.assertEditable(${locator1});

		

		
	}

	function assertEditableNotVisible(locator1) {
		WaitForSPARefresh();

		selenium.waitForElementPresent(${locator1}, "false");

		selenium.waitForEditable(${locator1});

		selenium.assertEditable(${locator1});

		

		
	}

}