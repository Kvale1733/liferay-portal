@default = "select"
definition {

	function pauseSelect(locator1, value1) {
		WaitForSPARefresh();

		selenium.pause(5000);

		selenium.select(${locator1}, ${value1});

		

		
	}

	function select(locator1, value1) {
		WaitForSPARefresh();

		selenium.waitForEditable(${locator1});

		selenium.mouseOver(${locator1});

		selenium.select(${locator1}, ${value1});

		

		
	}

}